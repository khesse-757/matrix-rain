services:
  web:
    build:
      context: .
      dockerfile: docker/Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - ./index.html:/app/index.html:ro
      - ./pyscript.toml:/app/pyscript.toml:ro
      - ./src:/app/src:ro
    restart: unless-stopped